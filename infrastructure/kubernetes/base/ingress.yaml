apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: alphatracer-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: alphatracer-service
            port:
              number: 8011
